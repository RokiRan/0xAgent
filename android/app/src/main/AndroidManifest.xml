<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oxagent.bus">

    <uses-sdk
        android:minSdkVersion="26"
        android:targetSdkVersion="28" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- registry 是 http:// 明文，targetSdk 28 默认禁止 cleartext，必须显式打开 -->
    <application
        android:label="0xBusAgent"
        android:debuggable="true"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 人脸检测可视化测试页：竖屏锁定，预览与检测框同坐标系 -->
        <activity
            android:name=".FaceTestActivity"
            android:screenOrientation="portrait"
            android:exported="false" />
        <!-- 语音识别测试页：按住说话，松开出结果 -->
        <activity
            android:name=".AsrTestActivity"
            android:screenOrientation="portrait"
            android:exported="false" />
        <!-- 手合并自 play-services-base：ML Kit 错误恢复对话框 -->
        <activity
            android:name="com.google.android.gms.common.api.GoogleApiActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:exported="false" />
        <!-- 手合并自 play-services-basement -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <!-- 手合并自 mlkit common/vision-common/text-common：组件注册发现，缺了 getClient NPE -->
        <service
            android:name="com.google.mlkit.common.internal.MlKitComponentDiscoveryService"
            android:exported="false">
            <meta-data
                android:name="com.google.firebase.components:com.google.mlkit.common.internal.CommonComponentRegistrar"
                android:value="com.google.firebase.components.ComponentRegistrar" />
            <meta-data
                android:name="com.google.firebase.components:com.google.mlkit.vision.common.internal.VisionCommonRegistrar"
                android:value="com.google.firebase.components.ComponentRegistrar" />
            <meta-data
                android:name="com.google.firebase.components:com.google.mlkit.vision.text.internal.TextRegistrar"
                android:value="com.google.firebase.components.ComponentRegistrar" />
            <meta-data
                android:name="com.google.firebase.components:com.google.mlkit.vision.face.internal.FaceRegistrar"
                android:value="com.google.firebase.components.ComponentRegistrar" />
        </service>

        <!-- 手合并自 mlkit common：ML Kit 组件初始化，缺了 getClient 会崩 -->
        <provider
            android:name="com.google.mlkit.common.internal.MlKitInitProvider"
            android:authorities="com.oxagent.bus.mlkitinitprovider"
            android:exported="false" />
        <!-- share_file 工具：工作目录文件对外授权分享 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.oxagent.bus.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".AgentService"
            android:exported="false" />
        <service
            android:name=".PhoneOperatorService"
            android:exported="true"
            android:label="0xBusAgent 手机操作"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/operator_config" />
        </service>
        <!-- 通知监听：settings put secure enabled_notification_listeners 授权后生效 -->
        <service
            android:name=".NotifyService"
            android:exported="true"
            android:label="0xBusAgent 通知监听"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
        <receiver
            android:name=".ScheduleReceiver"
            android:exported="false" />
    </application>
</manifest>
